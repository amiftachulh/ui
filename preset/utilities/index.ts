import { animation } from "./animation";
import { focusRing } from "./focus-ring";
import { transform } from "./transform";

export const utilities = Object.assign({}, animation, focusRing, transform);
