---
title: Calendar
description: A date field component that allows users to enter and edit date.
links:
  - title: Docs
    href: https://daypicker.dev/
---

<ComponentPreview name="calendar-demo" />

## About

The `Calendar` component is built on top of [React DayPicker](https://daypicker.dev/).

## Installation

<Steps>

<Step>Install the following dependencies:</Step>

<PackageInstaller name="react-day-picker" />

<Step>Add the [Button](/docs/components/button) component to your project.</Step>

The `Calendar` component uses the `Button` component. Make sure you have it installed in your project.

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="calendar.tsx" type="component" />

</Steps>

## Usage

```tsx
import { Calendar } from "@/components/ui/calendar";
```

```tsx
const [date, setDate] = useState<Date>();

return (
  <Calendar
    mode="single"
    selected={date}
    onSelect={setDate}
    className={css({ w: "fit", borderWidth: "1px", rounded: "md" })}
  />
);
```

See the [React DayPicker](https://daypicker.dev/) documentation for more information.

## Date Picker

You can use the `<Calendar>` component to build a date picker. See the [Date Picker](/docs/components/date-picker) page for more information.

## Props

The props below are additional props that can be passed to the `Calendar` component. For a full list of props, see the [React DayPicker](https://daypicker.dev/) documentation.

<PropsTable
  data={[
    {
      name: "minYear",
      type: "number",
      default: "10 years before current year",
      description: "The minimum calendar year.",
    },
    {
      name: "maxYear",
      type: "number",
      default: "9 years after current year",
      description: "The maximum calendar year.",
    },
    {
      name: "showSwitcher",
      type: "boolean",
      default: "true",
      description: "Render a button to switch between years and months rather than labels.",
    },
  ]}
/>
