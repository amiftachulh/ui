---
title: Multi Select
description: Multi Select component is used to select multiple options from a list of options.
---

<ComponentPreview name="multi-select-demo" />

## Installation

<Steps>

<Step>Install the following dependencies:</Step>

<PackageInstaller name="@radix-ui/react-popover cmdk" />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="multi-select.tsx" type="component" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

## Usage

```tsx
import { MultiSelect } from "@/components/ui/multi-select";
```

```tsx
const [selectedFrameworks, setSelectedFrameworks] = useState<string[]>(["react", "angular"]);

<MultiSelect
  options={frameworksList}
  onValueChange={setSelectedFrameworks}
  defaultValue={selectedFrameworks}
  placeholder="Select frameworks"
  maxCount={3}
/>;
```
