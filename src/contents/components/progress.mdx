---
title: Progress
description: Displays an indicator showing the completion progress of a task, typically displayed as a progress bar.
links:
  - title: Docs
    href: https://radix-ui.com/primitives/docs/components/progress
---

<ComponentPreview name="progress-demo" />

## Installation

<Steps>

<Step>Install the following dependencies:</Step>

<PackageInstaller name="@radix-ui/react-progress" />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="progress.ts" type="slot-recipe" />

<ComponentSource name="progress.tsx" type="component" />

<Step>Import the recipe into the respective `index.ts` file.</Step>

```tsx
import { progressSlotRecipe } from "./progress";

export const slotRecipes = {
  progress: progressSlotRecipe,
};
```

<Step>Run `panda codegen` and update the import paths to match your project setup.</Step>

</Steps>

## Usage

```tsx
import { Progress } from "@/components/ui/progress";
```

```tsx
<Progress value={33} />
```
